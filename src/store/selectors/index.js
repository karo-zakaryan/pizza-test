import * as authSelectors from './authSelectors';
import * as bucketSelectors from './bucketSelectors';
import * as currencySelectors from './currencySelectors';
import * as pizzasSelectors from './dataSelectors';

export { authSelectors, bucketSelectors, currencySelectors, pizzasSelectors };
